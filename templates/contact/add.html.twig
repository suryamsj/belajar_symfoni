{% extends 'base.html.twig' %}

{% block body %}

	<div class="container mt-5">
		<div class="row">
			<div class="col-lg-12 col-md-12 col-12">
				<div class="card">
					<div class="card-header">
						Add new contact
						<a href="/contact" class="btn btn-primary btn-sm float-end">Back</a>
					</div>
					<div class="card-body">
						{{ form_start(form) }}

						{{ form_rest(form) }}
						<input type="submit" class="btn btn-primary"/>

						{{ form_end(form) }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
